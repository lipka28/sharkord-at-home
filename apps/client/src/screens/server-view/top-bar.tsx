import { memo } from 'react';

const TopBar = memo(() => {
  return (
    <header className="flex h-12 items-center border-b border-border bg-card px-4">
      test
    </header>
  );
});

export { TopBar };
